# Private dev workspace (nested git repo, separately tracked)
BawrSpam_Dev/

# Local git worktrees
.worktrees/

# Agent / tool layer (Claude Code, Codex)
.claude/
.codex/

# Agent-entry docs — canonical lives in BawrSpam_Dev/agent-entry/, hardlinked to root (STU-046)
CLAUDE.md
AGENTS.md

# WoW packager working files
.release/
.pkgmeta-cache/

# Libraries: vendored directly in-repo (no .pkgmeta externals — manual vendor pattern)
# Libs/ is tracked. Do not re-add this ignore.

# Build artifacts
*.zip
*.tar.gz

# Windows junk
Thumbs.db
nul
desktop.ini

# Editor / IDE
.vscode/
.idea/

# Brainstorm artifacts (visual companion server)
.superpowers/
*.swp
*.swo
*~

# Secrets
.env
.env.local
*.key
